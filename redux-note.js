

import * as Redux from 'redux';


const store = Redux.createStore( /* reducer */ )

store.dipatch( /* reducer */ );
store.getState();
store.dispatch( /* plainAction object */ );
store.subscribe( /* cb */ )
